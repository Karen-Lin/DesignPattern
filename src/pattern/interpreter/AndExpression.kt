package pattern.interpreter

import pattern.interpreter.data.PersonalInfo
import java.util.ArrayList

//TODO 1.implement Expression   2.實作 interpreter() 取得 expr1 和 expr2 的交集(intersect)
class AndExpression(private val exr1: Expression, private val exr2: Expression) {


    /*
    ArrayList<PersonalInfo>(exr1.interpreter(nameList))
    intersect(exr2.interpreter(nameList))
    toList()
    */
}